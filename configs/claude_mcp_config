# Claude MCP Configuration Commands
# Run these commands to set up your Model Context Protocol servers

# BASIC MCPs (Working and tested)
claude mcp add markitdown-simple npx markitdown-mcp-npx
claude mcp add filesystem npx @modelcontextprotocol/server-filesystem
claude mcp add sequential-thinking npx mcp-sequentialthinking-tools
claude mcp add macos-automator npx @steipete/macos-automator-mcp
claude mcp add wcgw wcgw_mcp
# NOTE: free-will-mcp does not exist - removed

# CREATIVE & AI MCPs (Require API keys)
# NOTE: @anthropic-ai/mcp-outsource does not exist - removed
claude mcp add huggingface npx @huggingface/mcp-client
claude mcp add ableton-live npx @xiaolaa2/ableton-copilot-mcp

# DATA & WEB MCPs (Require configuration)
claude mcp add supabase npx @supabase/mcp-server-supabase
claude mcp add gmail npx @gongrzhe/server-gmail-mcp-cloud
claude mcp add calendar npx @cocal/google-calendar-mcp
claude mcp add youtube-watchlater npx @kazuph/mcp-youtube
claude mcp add webscraper npx scraperis-mcp

# INTEGRATION MCPs
claude mcp add openapi npx @ivotoby/openapi-mcp-server
# NOTE: terminal-control-mcp does not exist - removed

# VERIFY INSTALLATION
# Run this command to see all configured MCPs:
# claude mcp list

# RESTART CLAUDE CODE
# After adding MCPs, restart Claude Code to activate them